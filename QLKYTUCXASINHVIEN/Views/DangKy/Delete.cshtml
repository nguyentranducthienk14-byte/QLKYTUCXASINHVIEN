

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger shadow">
                <div class="card-body text-center p-5">
                    <i class="fas fa-exclamation-triangle text-danger fa-4x mb-3"></i>
                    <h3 class="text-danger">Xác nhận xóa?</h3>
                    <p class="text-muted">Bạn có chắc chắn muốn xóa đơn đăng ký của sinh viên <strong>@Model.SinhVien.HoTen</strong>? Hành động này không thể hoàn tác.</p>

                    <ul class="list-group list-group-flush text-start mb-4">
                        <li class="list-group-item"><strong>Mã đơn:</strong> #@Model.MaDangKy</li>
                        <li class="list-group-item"><strong>Phòng:</strong> @Model.Phong.SoPhong</li>
                    </ul>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-danger btn-lg px-5 shadow">Xác nhận Xóa</button>
                        <a href="@Url.Action("Index")" class="btn btn-outline-secondary btn-lg">Hủy</a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>